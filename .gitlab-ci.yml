sonarqube-check:
  image: 
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script: 
    - sonar-scanner
  allow_failure: true
  rules:
    - if: $CI_COMMIT_BRANCH == 'main'

stages:
  - test

vitest_test:
  stage: test
  image: node:latest
  script:
    - npm install
    - npm run test:vitest // Make sure this script runs Vitest tests for your Client folder
  artifacts:
    when: always
    reports:
      junit: Client/test-results.xml // Update the path to match the new location
    expire_in: 1 week

